class Solution {
    /**
     * @param {number[]} nums
     * @return {number[][]}
     */
    threeSum(nums) {
        const n = nums.length;
        const output = new Array(n).fill(1);

        let assorted = num.sort()

    }

    // takemout funciton ality of main call
}